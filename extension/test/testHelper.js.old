// const jsdom = require("jsdom");
// const { JSDOM } = jsdom;
// const Twindow = (new JSDOM(``, { runScripts: 'dangerously', url: 'file:///Users/lukasoppermann/Code/html5sortable/' })).window;
// const doc = jsdom('<!doctype html><html><body></body></html>');
// const win = doc.defaultView;

// global.document = doc;
// global.window = win;

// Object.keys(window).forEach((key) => {
//   if (!(key in global)) {
//     global[key] = window[key];
//   }
// });